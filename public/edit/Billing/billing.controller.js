angular.module('lightShowApp')
  .controller('BillingCtrl', function($state, $firebaseArray, $timeout, Auth, Users, profile, projects, currentPage){
    var billingCtrl = this;

    billingCtrl.profile = profile;
    //console.log(billingCtrl.profile)

  

  });
